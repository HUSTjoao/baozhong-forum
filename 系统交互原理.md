# 用户交互系统原理说明

## 📋 系统架构概览

```
┌─────────────┐         ┌──────────────┐         ┌─────────────┐
│   浏览器    │  HTTP   │  Next.js API │  Prisma │  PostgreSQL │
│  (前端)     │ ──────> │   (后端)     │ ──────> │   (数据库)  │
└─────────────┘         └──────────────┘         └─────────────┘
```

## 🔄 完整数据流程

### 1. 用户注册流程

```
用户填写注册表单
    ↓
前端调用: POST /api/auth/register
    ↓
后端验证数据（账号、邮箱是否已存在）
    ↓
使用 bcrypt 加密密码
    ↓
Prisma 保存到数据库 (User 表)
    ↓
返回用户信息（不含密码）
    ↓
前端自动登录（NextAuth）
    ↓
Session 存储在 Cookie 中
```

**关键代码位置：**
- 前端：`app/auth/login/page.tsx` - `handleRegister` 函数
- 后端：`app/api/auth/register/route.ts`
- 数据库：`prisma/schema.prisma` - `User` 模型

### 2. 用户登录流程

```
用户输入账号/密码
    ↓
前端调用: NextAuth signIn('credentials')
    ↓
后端: lib/authOptions.ts - authorize 函数
    ↓
从数据库查询用户（通过 username 或 email）
    ↓
使用 bcrypt 验证密码
    ↓
创建 JWT Token（包含用户信息）
    ↓
Token 存储在 Cookie 中
    ↓
前端通过 useSession() 获取用户信息
```

**关键代码位置：**
- 前端：`app/auth/login/page.tsx` - `handleLogin` 函数
- 后端：`lib/authOptions.ts` - `authorize` 函数
- 认证：NextAuth.js 管理 Session

### 3. 发布问题/观点流程

```
用户在发帖页面填写内容
    ↓
前端调用: questionsApi.create()
    ↓
发送 POST /api/questions
    ↓
后端验证用户登录状态（getServerSession）
    ↓
检查用户是否被禁言（从数据库查询）
    ↓
Prisma 创建 Question 记录
    ↓
返回创建的问题数据
    ↓
前端刷新页面，显示新问题
```

**关键代码位置：**
- 前端：`lib/api-client.ts` - `questionsApi.create()`
- 后端：`app/api/questions/route.ts`
- 数据库：`prisma/schema.prisma` - `Question` 模型

### 4. 发表回复流程

```
用户在问题详情页输入回复
    ↓
前端调用: repliesApi.create()
    ↓
发送 POST /api/questions/[id]/reply
    ↓
后端验证用户登录状态
    ↓
检查用户是否被禁言
    ↓
Prisma 创建 Reply 记录
    ↓
更新问题的 repliesCount
    ↓
返回更新后的问题（包含新回复）
    ↓
前端更新 UI，显示新回复
```

**关键代码位置：**
- 前端：`lib/api-client.ts` - `repliesApi.create()`
- 后端：`app/api/questions/[id]/reply/route.ts`
- 数据库：`prisma/schema.prisma` - `Reply` 模型

### 5. 点赞流程

```
用户点击点赞按钮
    ↓
前端调用: questionsApi.toggleLike(id)
    ↓
发送 POST /api/questions/[id]/like
    ↓
后端验证用户登录状态
    ↓
检查用户是否被禁言
    ↓
查询是否已点赞（QuestionLike 表）
    ↓
如果已点赞：删除点赞记录，likes - 1
    ↓
如果未点赞：创建点赞记录，likes + 1
    ↓
使用 Prisma Transaction 确保数据一致性
    ↓
返回更新后的问题数据
    ↓
前端更新 UI，显示新的点赞数
```

**关键代码位置：**
- 前端：`lib/api-client.ts` - `questionsApi.toggleLike()`
- 后端：`app/api/questions/[id]/like/route.ts`
- 数据库：`prisma/schema.prisma` - `QuestionLike` 模型

## 🔐 认证机制

### NextAuth.js Session 管理

1. **登录时：**
   - 用户凭据验证成功后，NextAuth 创建 JWT Token
   - Token 包含：用户 ID、邮箱、角色等信息
   - Token 存储在 HTTP-only Cookie 中（安全）

2. **后续请求：**
   - 浏览器自动发送 Cookie
   - 后端通过 `getServerSession(authOptions)` 获取用户信息
   - 无需每次请求都验证密码

3. **前端获取用户信息：**
   ```typescript
   const { data: session } = useSession()
   // session.user.id - 用户 ID
   // session.user.role - 用户角色
   // session.user.nickname - 用户昵称
   ```

## 💾 数据存储机制

### 数据库表结构

1. **User 表** - 存储用户信息
   - id, email, username, password (加密)
   - role, universityId, graduationYear, major
   - nickname, bio, avatarUrl, isMuted

2. **Question 表** - 存储问题/观点
   - id, title, content, askerId
   - universityId, majorId, category
   - likes, repliesCount, date

3. **Reply 表** - 存储回复
   - id, questionId, content, replierId
   - parentReplyId (支持嵌套回复)
   - likes, date

4. **QuestionLike / ReplyLike / AlumniMessageLike** - 存储点赞关系
   - questionId/replyId/messageId, userId
   - 用于追踪谁点赞了什么

### 数据同步原理

**关键：所有数据都存储在 PostgreSQL 数据库中**

1. **不同浏览器访问：**
   - 用户 A 在浏览器 1 登录 → 数据保存到数据库
   - 用户 A 在浏览器 2 登录 → 从数据库读取相同数据
   - ✅ 数据完全同步

2. **多用户交互：**
   - 用户 A 发布问题 → 保存到数据库
   - 用户 B 刷新页面 → 从数据库读取，看到用户 A 的问题
   - ✅ 实时共享

3. **数据持久化：**
   - 所有操作都通过 Prisma 写入数据库
   - 数据库在服务器上，不依赖浏览器
   - ✅ 数据永久保存

## 🔄 API 调用流程示例

### 示例：用户发布问题

```typescript
// 1. 前端代码（app/forum/ask/page.tsx）
const handleSubmit = async () => {
  // 调用 API 客户端
  const question = await questionsApi.create({
    title: '我的问题',
    content: '问题内容',
    isAnonymous: false,
    universityId: 'xxx',
  })
  
  // 跳转到问题详情页
  router.push(`/forum/${question.id}`)
}

// 2. API 客户端（lib/api-client.ts）
export const questionsApi = {
  async create(questionData) {
    const res = await fetch('/api/questions', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(questionData),
    })
    const data = await res.json()
    return data.question
  }
}

// 3. 后端 API（app/api/questions/route.ts）
export async function POST(req: NextRequest) {
  // 验证登录
  const session = await getServerSession(authOptions)
  if (!session?.user?.id) {
    return NextResponse.json({ error: '请先登录' }, { status: 401 })
  }
  
  // 检查禁言状态
  const user = await prisma.user.findUnique({
    where: { id: session.user.id },
    select: { isMuted: true },
  })
  
  // 创建问题
  const question = await prisma.question.create({
    data: {
      title: body.title,
      content: body.content,
      askerId: session.user.id,
      // ...
    },
  })
  
  return NextResponse.json({ question })
}

// 4. 数据库操作（Prisma）
// Prisma 自动将数据写入 PostgreSQL
```

## 🎯 关键设计点

### 1. 前后端分离
- 前端：React + Next.js（客户端组件）
- 后端：Next.js API Routes（服务端）
- 通信：HTTP RESTful API

### 2. 统一 API 客户端
- `lib/api-client.ts` 封装所有 API 调用
- 统一的错误处理
- 类型安全（TypeScript）

### 3. 数据库驱动
- 所有数据存储在 PostgreSQL
- Prisma ORM 管理数据库操作
- 支持事务、关联查询等高级功能

### 4. 安全机制
- 密码使用 bcrypt 加密
- Session 存储在 HTTP-only Cookie
- 每次 API 请求验证用户身份
- 权限检查（管理员、禁言用户等）

## 📊 数据流图

```
┌─────────────────────────────────────────────────────────┐
│                     用户操作                            │
│  (注册/登录/发帖/回复/点赞)                             │
└──────────────────┬────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────┐
│              前端 (React Components)                    │
│  - 收集用户输入                                          │
│  - 调用 API 客户端                                       │
│  - 更新 UI                                               │
└──────────────────┬────────────────────────────────────┘
                   │ HTTP Request
                   ↓
┌─────────────────────────────────────────────────────────┐
│            API 客户端 (lib/api-client.ts)               │
│  - 封装 fetch 调用                                      │
│  - 统一错误处理                                          │
│  - 类型定义                                              │
└──────────────────┬────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────┐
│          Next.js API Routes (app/api/*)                  │
│  - 验证用户身份 (getServerSession)                       │
│  - 验证权限 (检查禁言、角色等)                           │
│  - 业务逻辑处理                                          │
└──────────────────┬────────────────────────────────────┘
                   │
                   ↓
┌─────────────────────────────────────────────────────────┐
│              Prisma ORM (lib/prisma.ts)                  │
│  - 数据库查询/更新                                       │
│  - 数据验证                                              │
│  - 事务管理                                              │
└──────────────────┬────────────────────────────────────┘
                   │ SQL
                   ↓
┌─────────────────────────────────────────────────────────┐
│            PostgreSQL 数据库                            │
│  - 持久化存储                                            │
│  - 数据一致性                                            │
│  - 支持多用户并发                                        │
└─────────────────────────────────────────────────────────┘
```

## 🔍 调试技巧

### 查看数据流

1. **浏览器开发者工具：**
   - Network 标签：查看所有 API 请求
   - Console 标签：查看前端日志
   - Application 标签：查看 Cookie/Session

2. **服务器日志：**
   ```bash
   pm2 logs baozhong-forum
   # 查看所有 API 请求的日志
   ```

3. **数据库查询：**
   ```bash
   npx prisma studio
   # 可视化查看数据库内容
   ```

## ✅ 总结

**用户交互的核心原理：**

1. ✅ **前端收集用户操作** → React 组件
2. ✅ **通过 API 发送请求** → fetch / API 客户端
3. ✅ **后端验证和处理** → Next.js API Routes
4. ✅ **数据库持久化** → Prisma + PostgreSQL
5. ✅ **返回结果更新 UI** → React 状态更新

**数据同步的关键：**
- 所有数据存储在**服务器数据库**中
- 不同浏览器/设备访问**同一个数据库**
- 因此数据**自动同步**，无需额外处理

这就是为什么用户在不同浏览器登录，能看到相同数据的原因！

